{% extends "layouts.master" %}

{% block content %}
  {% if components is defined and components|length %}
    {% for component in components %}
      {% component component %}
    {% endfor %}
  {% else %}
    {% component home %}
  {% endif %}
{% endblock %}
