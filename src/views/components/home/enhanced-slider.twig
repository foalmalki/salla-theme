<section style="padding:32px;background:#f8fafc;border-bottom:1px solid #e5e7eb">
  <div class="container">
    <h1 style="margin:0 0 8px">{{ theme.settings.hero_title|default('home.hero_title'|trans) }}</h1>
    <p style="margin:0">{{ theme.settings.hero_subtitle|default('home.hero_subtitle'|trans) }}</p>
  </div>
</section>
